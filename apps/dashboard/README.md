# Dashboard

Keep your existing dashboard here:

apps/dashboard/frontend

Build output should go to:
- dist/ (Vite)
or
- build/ (CRA)

Nginx serves the build output.
API is available at /api/* via gateway.conf.