import React from "react";

export default function IamBindingsPage() {
    return (
        <div className="rounded-2xl border border-black/5 bg-white p-5">
            <div className="text-lg font-semibold">IAM Bindings</div>
            <div className="mt-2 text-sm text-neutral-500">
                Next: scope picker (team/project) + grant/revoke bindings UI.
            </div>
        </div>
    );
}
