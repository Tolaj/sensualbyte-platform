# Provisioner tests (WIP)

We will add tests here after the provisioner modules are fully implemented.

Goal:
- test docker connectivity
- test ensureImage
- test ensureNetwork
- test ensureVolume
- test ensureCompute create/start/stop/remove
- test execInContainer output capture
- later: nginx gateway + minio + postgres