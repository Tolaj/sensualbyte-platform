// Optional module (ssh-router box / jumpbox convenience).
// v1: not used yet. Keep as placeholder.
export async function ensureSshRouter(_opts) {
    return { enabled: false, note: "ssh-router not implemented in v1" };
}
