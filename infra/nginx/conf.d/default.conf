server {
  listen 80 default_server;
  server_name _;

  # health / sanity
  location = / {
    return 200 "nginx up\n";
  }

  # keep a fallback to avoid weird resets
  location / {
    return 404;
  }
}
